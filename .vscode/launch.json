{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run FollowGap",
            "type": "debugpy",
            "request": "launch",
            "module": "FollowGap.control.fsm",
            "cwd": "${workspaceFolder}"
        }
    ]
}